aws()
{
	[ $vflag == 0 ] || echo +aws "$@" 1>&2
	exec 3>&1
	/usr/local/bin/aws ${DEBUG} \
		${NO_VERIFY_SSL} \
		${ENDPOINT_URL:+--endpoint-url $ENDPOINT_URL} \
		"$@" 2>&1 1>&3 3>&- | grep -v "InsecureRequestWarning:" 1>&2
}
